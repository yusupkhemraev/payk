version: "2"

linters:
  default: standard
  enable:
    - misspell
    - unconvert
    - unparam
  settings:
    staticcheck:
      checks: ["all", "-ST1000"]

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
